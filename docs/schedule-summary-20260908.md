# 日程总结与右键完成

日历任务卡片移除左侧完成圆圈，保留右键菜单“标记完成”和“恢复未完成”。原有拖动、点击编辑及空白时间段拖动新建不变。

编辑框的“备注”改为“总结”，继续使用原 `note` 字段，已有内容无需迁移。非空总结显示在卡片信息下方，14px 字号、保留换行，最多三行；完整内容仍可点开编辑框查看。

缩放显示按实际卡片空间判断，不依赖浏览器缩放百分比。卡片内容区至少 120px 宽、120px 高时显示总结，空间不足时隐藏；不会删除已保存的总结。使用容器查询，让页面缩放或尺寸变化后自动调整。

生产构建与 16 项 Node 测试通过；离线浏览器验证总结保存、右键完成及恢复、圆圈移除、大卡片显示、短卡片及窄卡片隐藏。原有日程、任务复盘、协助人、产出及桌面日程测试继续通过。公网 HTTPS 验证日程、总结输入标签、旧圆圈移除以及已有交互；桌面路由正常。

部署镜像 `tongpin-team-workspace:20260908-summary`，发布目录 `/opt/apps/tongpin-team-workspace/releases/20260908-summary`。上一版保留为停用容器 `tongpin-before-summary`。本次未更改用户数据；保留工作区中其他任务的桌面快捷方式删除及未跟踪目录，不将它们纳入本次提交。
