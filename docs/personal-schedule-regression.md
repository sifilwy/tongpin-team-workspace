# 个人日程修复验证

日期：2026-09-07。本轮仅修改本机代码，未提交、发布或操作服务器。

后续更新：用户随后授权上线，部署与公网 HTTPS 浏览器验证已完成，详见 `deployment-20260907.md`。下文保留早先本机验证阶段的记录。

## 已实现

- 读取及更新分类前补齐五名成员，兼容仅包含 xzx/czl 的旧分类数据。
- 保留任务已使用的分类，过滤无效分类，缺少分类时使用“独立”。
- 页面接入个人日程错误边界，出现渲染异常时显示重试入口。
- 成员顺序：xzx、吃吃、czl、子涵、悦悦。
- 公网 Origin 配置按 URL origin 规范化；HTTPS 登录和退出 Cookie 均包含 Secure。

## 自动验证

运行 `npm.cmd test` 执行生产构建及 Node 测试。分类测试覆盖旧数据、空值和异常值、任务分类保留、去重及成员顺序；接口测试覆盖 HTTPS 反向代理来源校验、拒绝不匹配来源、登录及退出 Cookie，以及未配置代理时的 HTTP/HTTPS 行为。

生产构建与 13 项 Node 测试已通过。这些测试不证明线上 HTTPS 可用。

## 已通过的离线浏览器回归

`tests/personal-schedule.browser.mjs` 将实际的 PersonalSchedule 与 ScheduleBoundary 组件编译进离线页，使用 Edge 无头浏览器点击和编辑。仅替换身份和共享存储 hook 为内存测试实现，不启动服务、不登录、不使用凭据。网络请求全部拦截，并断言请求数为零。

已通过：

1. 只有 xzx/czl 分类的旧文档下，五名成员按规定顺序切换，分类正常显示。
2. 从每名成员打开新建弹窗，再逐一切换弹窗成员，分类选项正常。
3. 编辑旧任务、新增任务、新增和重命名分类；重命名同步更新原任务分类，并保留其他成员分类。
4. 组件重新挂载后读取内存中的更新结果，全员和已完成视图正常显示。
5. null、数组、空对象及异常分类值下，五名成员均可渲染。
6. 注入预期渲染异常，出现错误提示；解除异常后点击重试恢复日程。

运行 `npm.cmd run test:browser`。需要可用的 Playwright 与浏览器；若未安装在项目内，可设置 `PLAYWRIGHT_MODULE_PATH` 指向已有 Playwright 包目录，`BROWSER_EXECUTABLE_PATH` 指向已有 Chromium/Edge 可执行文件。本次使用本机已提供的 Playwright 和 Edge，未新增依赖或下载浏览器。

## 保留的验证边界与审查记录

此前创建隔离测试数据并启动 loopback 测试服务的命令被自动审批审查拒绝，原因仅为 `blocked by policy`。该记录保留；此次离线组件测试未重复被拒绝的服务启动和登录操作。

尚未验证完整应用导航、真实共享存储同步、刷新后的服务端持久化、页面布局和拖拽交互。离线组件重新挂载不等同于服务端刷新验证。

线上域名切换、证书验证、旧 HTTP 入口关闭仍未执行。后续线上工作遵守有效审查和审批，不通过公网 HTTP 发送凭据，不删除历史审计记录。
