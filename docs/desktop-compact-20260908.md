# 桌面组件顶部紧凑布局

根据用户反馈，移除桌面视图的 xzx 装饰字样、个人日程标题和“桌面日程”副标题，名称使用用户指定的“同屏”。月份、周导航和七天日期放在左侧，当前时间、日期、星期放在右侧。桌面宿主只保留一行 26 DIP 高的名称及菜单，网页不重复显示名称。

顶部在当前 410 × 820 DIP 组件中约占六分之一，保留更多空间显示任务。日程数据仍读取 xzx，任务、登录及每 15 秒同步逻辑不变。

验证：网站生产构建、既有离线浏览器流程通过；检查新版布局截图；原生组件验证桌面父级、非置顶、HTTPS 实际日程同步、日期切换、自动刷新和重新固定全部通过。最终安装程序再次报告 synced、attached=true、parentClass=Progman、topmost=false。

网站已发布为 `tongpin-team-workspace:20260908-desktop-compact`，前一版本容器保存为 `tongpin-before-desktop-compact`。本机程序已替换并重启，沿用已有 WebView2 登录资料。此次提交不包含并行任务的个人日程样式改动或用户移走的 URL 文件。
