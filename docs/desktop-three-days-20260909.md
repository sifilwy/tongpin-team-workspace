# 桌面日程近三天视图

新增可见的“单日 / 近三天”切换。近三天以今天为起点，按今天、明天、后天分组；跨月正常显示，无任务的日期保留紧凑的“暂无安排”。视图偏好保存在本机 localStorage。点击日历日期或周导航切回单日，点击近三天回到以今天开始的范围。

删除顶部下一项/接下来/当日首项卡片及相关 CSS。任务列表保留当前时段高亮、完成状态、自动同步及待安排内容。

验证：隔离的生产构建通过；专用离线浏览器测试覆盖跨月三日分组、空日期、偏好保存、单日返回、自动刷新及范围过滤；公网 HTTPS 验证切换、刷新后记住视图及无下一项卡片通过。桌面宿主已重新打开并沿用软件渲染修复。

发布使用已提交的 HEAD 快照，仅覆盖本次桌面页面和样式，避免带入其他任务未提交的个人日程修改。生产镜像为 `tongpin-team-workspace:20260909-desktop-three`，旧容器保存为 `tongpin-before-desktop-three`。初次切换时镜像尚未就绪，回滚保护恢复旧容器；随后确认镜像构建完成，再成功切换并验证。
